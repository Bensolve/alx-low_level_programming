#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @num1: the first character
 * @num2: the second character
 *
 * Return: res is equal to num1 + num2
 */
int add(int num1, int num2)
{
	int res;

	res = num1 + num2;
	return (res);
}
