#include <stdio.h>

/**
 * main - to print text followed by a new line
 *
 * Return: Always 0
 */
int main(void)
{
	char text[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", text);
	return (0);
}
